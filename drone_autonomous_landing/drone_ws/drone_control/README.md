# package: drone_control

간단 소개
- `drone_control` 패키지는 시뮬레이션에서 드론의 제어 파이프라인(토픽 인터페이스, 컨트롤러 연결 등)을 제공합니다.

사용법
- 워크스페이스 루트에서 빌드 후 노드를 실행하세요. 예: `ros2 run <package> <executable>` (패키지 내 노드에 따라 다름).

관리자
- Junsuk Kim <bob4587@naver.com>
